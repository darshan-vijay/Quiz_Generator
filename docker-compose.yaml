version: "3"
services:
  server:
    build:
      context: .
      dockerfile: Dockerfile.Server
    ports:
      - "3001:3001"
    env_file:
      - .env
  frontend:
    build:
      context: ./react
      dockerfile: Dockerfile.Frontend
    ports:
      - "3000:80"
    env_file:
      - .env
