{
  "name": "google-forms-api-server",
  "version": "1.0.0",
  "description": "API server for Google Forms integration",
  "main": "server.ts",
  "scripts": {
    "start": "node -r ts-node/register server.ts",
    "dev": "nodemon --exec 'ts-node --transpile-only' server.ts",
    "build": "tsc",
    "test": "jest"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.17",
    "@types/node": "^18.19.86",
    "nodemon": "^2.0.22",
    "ts-node": "^10.9.2",
    "typescript": "^5.0.4"
  }
}
